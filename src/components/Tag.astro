---
const { tag, active } = Astro.props;
---
<li><a href={`/tags/${tag}`} class={`${active ? 'bg-slate-500' : 'hover:text-blue-500 transition-colors duration-200'} block p-2 mr-2 mb-2  text-sm rounded-xl font-medium`}>#{tag}</a></li>