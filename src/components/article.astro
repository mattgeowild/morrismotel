---
import type { Post } from '@data/post';
const post: Post = Astro.props.post
---

<div class="py-6 flex flex-row-reverse gap-6 md:gap-10 items-start">
	{post.img &&
		<a href={`/posts/${post.slug}`} class="shrink-0">
			<img src={post.img} alt={post.title} width="160" height="160" class="w-32 h-24 rounded bg-base-200 object-cover" />
		</a>
	}
	<div class="flex flex-col gap-4">
		<h2 class="text-2xl font-bold">
			<a class="hover:underline" href={`/posts/${post.slug}`}>{post.title}</a>
		</h2>
		<p class="text-sm text-base-content/70">{post.desc}</p>
	</div>
</div>