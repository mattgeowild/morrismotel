---
const { date } = Astro.props.post;
const formattedDate = new Intl.DateTimeFormat('en-US', { year: 'numeric', month: 'long', day: 'numeric' }).format(Date.parse(date))
---
<time datetime={date}>{formattedDate}</time>